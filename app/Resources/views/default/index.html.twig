{% extends 'base.html.twig' %}


{% block body %}

<div class="row">
    <div class="col-lg-12">
        {% for date, activities in activitiesByDates %}
            {{ include('Activity/list.html.twig', { 'activities': activities.activities, 'title': date | date('D d M Y'), 'tags': activities.tags, 'readOnly': false }) }}
        {% endfor %}
    </div>
</div>
{% endblock %}
