<{{ type | default('a') }} {% if(url | default(null)) %} href="{{ url }}" {% endif %} title="{{ tag.name }}" style="color: {{ tag.colorText }}; {% if(tag.color) %}border: 1px solid {{ tag.color }}; background: {{ tag.color }};{% endif %}" class="btn btn-tag btn-{{ size | default('sm') }} btn-default">{% if(tag.icon) %}<span class="glyphicon glyphicon-{{ tag.icon }}"></span> {% endif %}{{ tag.name }} {% if(badge | default(false)) %}&nbsp;<span class="badge" style="color: {{ tag.color }}; background: {{ tag.colorText }};">{{ badge }}</span> {% endif %} </{{ type | default('a') }}>
