<{{ type | default('a') }} {% if(url | default(null)) %} href="{{ url }}" {% endif %} title="{{ tag.name }}" style="{% if(tag.colorText) %}color: {{ tag.colorText }};{% endif %} {% if(tag.color) %}border-color: {{ tag.color }}; background: {{ tag.color }}{% endif %}" class="btn btn-tag btn-{{ size | default('sm') }} btn-default">{% if(tag.icon) %}<span class="glyphicon glyphicon-{{ tag.icon }}"></span> {% endif %}{{ tag.name }} {% if(badge | default(false)) %}&nbsp;<span class="badge" style="{% if(tag.color) %} color: {{ tag.color }}; background: #fff;{% endif %}">{{ badge }}</span> {% endif %} </{{ type | default('a') }}>
